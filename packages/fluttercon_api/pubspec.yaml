name: fluttercon_api
description: A package managing API calls for FlutterCon demo app.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.4.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.7
  very_good_analysis: ^6.0.0
dependencies:
  fluttercon_data_source:
    path: ../../api/packages/fluttercon_data_source
  fluttercon_shared_models:
    path: ../../api/packages/fluttercon_shared_models
  http: ^1.2.2
  user_repository:
    path: ../../api/packages/user_repository
